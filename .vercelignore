# Vercel is invoked at the repository root so it can resolve the project's
# apps/web Root Directory. Only the web application is needed for that build;
# excluding the .NET build outputs keeps the deployment below Vercel's file
# size limit.
*
!apps/
!apps/web/
!apps/web/**
