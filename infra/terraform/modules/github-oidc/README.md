# GitHub OIDC module

Creates one user-assigned managed identity, exact-subject GitHub federated credentials, and scoped RBAC assignments. Create separate module instances for plan, image push, and apply responsibilities; do not combine those permissions into one identity in new environments.
