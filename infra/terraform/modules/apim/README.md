# API Management module

Creates the long-lived APIM service. API definitions, backend bindings, and policies belong to the runtime stack because they change with application routes. The POC uses Consumption; the production reference selects Standard v2 and composes outbound networking at the environment root.
