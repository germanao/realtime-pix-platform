# Workload Identity

Creates one user-assigned managed identity and its explicit RBAC assignments.
Callers should scope roles to a queue, topic, subscription, vault, registry, or
configuration store instead of granting namespace or resource-group access.
