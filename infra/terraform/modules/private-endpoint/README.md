# Private endpoint module

Creates one approved private endpoint and binds it to one or more private DNS zones. Callers must supply the service-specific subresource name, such as `namespace`, `signalr`, `registry`, or `vault`.
