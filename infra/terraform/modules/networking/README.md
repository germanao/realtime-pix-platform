# Networking module

Creates a VNet, purpose-specific delegated subnets, private DNS zones, and links. The production reference allocates separate subnets for Container Apps infrastructure, APIM outbound integration, PostgreSQL delegation, and private endpoints. The POC deliberately does not instantiate this module.
